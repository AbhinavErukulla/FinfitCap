PORT=3000
# Use NODE_ENV=production only when deploying; production enables rate limits on /login and /register.
NODE_ENV=development

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=12345678
DB_NAME=findit

JWT_SECRET=FindItSecureJWTSecretKey2026!@#
JWT_EXPIRES_IN=8h
JWT_REFRESH_EXPIRES_IN=7d

FRONTEND_ORIGIN=http://localhost:3000
